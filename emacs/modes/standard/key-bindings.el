;;; Regular key-bindings

(mt/bind-keys '(
                ;; Misc
                ("C-c )" . theme-looper-enable-next-theme)
                ("C-c (" . theme-looper-enable-previous-theme))
              global-map)
